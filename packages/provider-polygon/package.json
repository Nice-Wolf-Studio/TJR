{
  "name": "@tjr/provider-polygon",
  "version": "0.0.1",
  "description": "Polygon.io market data provider adapter",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "node --test tests/*.test.js",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "keywords": [
    "tjr",
    "polygon",
    "market-data",
    "provider"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "@tjr-suite/market-data-core": "workspace:*",
    "@tjr/contracts": "workspace:*",
    "@tjr/logger": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^18.19.0"
  }
}
