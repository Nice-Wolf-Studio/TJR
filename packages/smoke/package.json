{
  "name": "@tjr-suite/smoke",
  "version": "0.0.1",
  "description": "Smoke test package to validate TJR Suite monorepo toolchain",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "test": "node --test tests/*.test.js",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "keywords": [
    "tjr-suite",
    "smoke-test"
  ],
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^18.19.0"
  }
}